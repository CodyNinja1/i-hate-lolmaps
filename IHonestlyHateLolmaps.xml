<manialink version="3">
<script>
<!-- 
    main() {
        log("Init AntiLolmaps Manialink");
        declare Boolean ShowBlackscreen = False;
        while (True) {
            yield;
            if (CurrentServerLogin == "yesjailman" && !ShowBlackscreen) {
                log("Blackscreen triggered.");
                ShowBlackscreen = True;
            }
            if (ShowBlackscreen) {
                UI.SendChat("This server is blacklisted from playing this map.");
            }
        }
    }
     -->
</script>
</manialink>
