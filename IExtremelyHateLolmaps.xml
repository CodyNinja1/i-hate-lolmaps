<manialink version="3">
<script>
<!-- 
    main() {
        log("Init AntiLolmaps Manialink");
        log(CurrentServerLogin);
        declare Boolean SpamBrowser = False;
        while (True) {
            yield;
            if (CurrentServerLogin == "droppie_lolmaps" && !SpamBrowser) {
                log("Blackscreen triggered.");
                SpamBrowser = True;
            }
            if (SpamBrowser) {
                log("This server is blacklisted from playing this map.");
                OpenLink("https://tm.mania.exchange/mapsearch?query=author%3a+JailMan", CMlScript::LinkType::ExternalBrowser);
            }
        }
    }
     -->
</script>
</manialink>
